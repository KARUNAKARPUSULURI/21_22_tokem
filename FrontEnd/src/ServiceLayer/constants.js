const baseurl = "http://localhost:3000";

const getProductsUrl = `${baseurl}/products`
const postRegisterUrl = `${baseurl}/register`
const postLoginUrl = `${baseurl}/login`

export {getProductsUrl, postRegisterUrl, postLoginUrl}